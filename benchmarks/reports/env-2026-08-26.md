# Environment Proof — T-00 (Phase 0)

Date: 2026-08-26 · Machine: Windows, CPU-only · Interpreter: CPython 3.13.7
Command evidence: `pytest` → 6 passed · `scrydb check` → PASS (exit 0) · `scrydb check --full` → PASS (exit 0)

## Layer results

| Layer | Result | Detail |
|---|---|---|
| Python | OK | 3.13.7 |
| SQLite (bundled) | OK | 3.50.4 |
| FTS5 | OK | virtual table + `bm25()` MATCH query succeeded |
| sqlite-vec | OK | v0.1.9; loadable-extension loading works on this build; 4-dim KNN smoke query returned 2 rows |
| Embedding stack | OK | sentence-transformers installed; `sentence-transformers/all-MiniLM-L6-v2` downloaded and loaded, **dimension = 384** |

## Issues found & fixed during proof

1. FTS5 probe originally used a schema-qualified name (`bm25(temp.probe_fts)`) — invalid in expression position ("no such column"). Fixed by probing on an unqualified throwaway `:memory:` table.
2. sentence-transformers deprecation: `get_sentence_embedding_dimension` → accessor fallback added.

## Notes

- HF Hub download worked unauthenticated; a token would speed future model fetches (not required).
- Conclusion per ADR-2/ADR-3: the pure-SQLite architecture is viable on this machine with the CPU-first embedder; Phase 1 may proceed.
